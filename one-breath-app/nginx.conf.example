# Nginx configuration for onebreath.socials
# Place this in /etc/nginx/sites-available/onebreath.socials

server {
    listen 80;
    server_name onebreath.socials www.onebreath.socials;

    root /var/www/onebreath.socials/dist;
    index index.html;

    # SPA routing - redirect all requests to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
}

# HTTPS configuration (after Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name onebreath.socials www.onebreath.socials;
#
#     ssl_certificate /etc/letsencrypt/live/onebreath.socials/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/onebreath.socials/privkey.pem;
#
#     root /var/www/onebreath.socials/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /assets {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }

